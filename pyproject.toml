[project]
name = "expense-tracekr-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "digam-hue", email = "digubaditya@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=4.0.3",
    "aiosqlite>=0.20.0",
]

[project.scripts]
expense-tracekr-mcp-server = "expense_tracekr_mcp_server:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
